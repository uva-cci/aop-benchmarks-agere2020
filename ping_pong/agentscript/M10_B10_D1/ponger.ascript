!init.

+!init =>
    N = "pinger" + #executionContext.name.replaceAll("ponger","");
    +neighbour(N).

+!pong(D) : neighbour(N) =>
        #println(N);
        #println("start waiting");
        #wait_actions.actions.waitForSomeTime(#asDouble(D));
        #achieve(N, finished)
.








