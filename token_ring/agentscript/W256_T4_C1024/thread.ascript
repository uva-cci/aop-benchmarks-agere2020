// parameters --
// number of agents: 256

!init.

+!init =>
    N = "thread" + ((#executionContext.name.replaceAll("thread","").toInt % 256) + 1);
    +neighbor(N).

+!token(0) =>
    #achieve("master1", done).

+!token(T) : neighbor(N) =>
    #achieve(N, token(T - 1)).







